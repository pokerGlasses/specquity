# Optional: Implementation for a small 128x64 I2C OLED
def update_hud(win_rate, stage):
    # Use Adafruit_SSD1306 library to draw text
    # display.text(f"Win: {win_rate}%", 0, 0)
    # display.text(f"Stage: {stage}", 0, 20)
    pass